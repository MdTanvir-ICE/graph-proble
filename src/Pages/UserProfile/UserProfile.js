import React from 'react';

const UserProfile = () => {
    return (
        <div style={{display:'flex',justifyContent:'center',alignItems:'center',fontSize:'30px',color:'green'}}>
            this page for user profile
        </div>
    );
};

export default UserProfile;